<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 深色主题底色，与网页 UI 保持一致，避免加载时白屏闪烁 -->
    <!-- 用系统原生主题，不依赖 AppCompat -->
    <style name="Theme.Futuresterminal" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">#0d1117</item>
    </style>
</resources>
